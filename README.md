# ProjCC

To run the program, just run the main.py file, it will print the time it took to calculate all coordinates in both the C-matrix format and Z-matrix format.
Some new longer test zmat files still need to be added.
